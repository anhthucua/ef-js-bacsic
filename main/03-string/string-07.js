// 07 thay the param bang value tuong ung

export function replaceValue(str, a, b) {
  if (str === '') return {};

  if (!str.includes(a)) {
    return {};
  }

  // const replaceStr = str.replaceAll(a, b);
  // return replaceStr;
}

// onsole.log('trananhthu'.replaceAll('thu', 'thoi'));c